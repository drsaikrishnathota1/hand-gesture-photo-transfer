const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const h=fs.readFileSync(path.join(__dirname,'..','public','intelligence.html'),'utf8');const j=fs.readFileSync(path.join(__dirname,'..','public','intelligence.js'),'utf8');test('V6 decision cockpit exists',()=>{assert.match(h,/Who → Where → What should we test/);assert.match(h,/AUDIENCE OPPORTUNITY/);assert.match(h,/MARKET QUALITY/);assert.match(h,/EXECUTIVE DECISION/);assert.match(h,/GLOBAL COMPARE/)});test('V6 renders live decision metrics',()=>{assert.match(j,/renderDecisionCockpitV6/);assert.match(j,/Reach/);assert.match(j,/Engagement/);assert.match(j,/Volume/)});